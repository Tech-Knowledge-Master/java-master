private, default, protected, public가 존재합니다.

private은 해당 클래스 내에서만 접근 가능
default는 해당 패키지 내에서 접근 가능
protected는 상속한 클래스에서 접근 가능
public은 전체 영역에서 접근 가능

접근 제어자를 사용하는 이유는 
외부에 보여주고 싶은 정보들을 선택적으로 제공하기 위함 ( 캡슐화 )